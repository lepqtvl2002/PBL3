using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace PBL3_Tutor.Controllers
{
    public class Manage_account<t>
    {
        /*public List<Account<t>> List_Accounts;
        public List<t> List_t;
        public List<t> Search(string s)
        {
            List<t> list = new List<t>();
            if (s == "Lớp")
            {
                foreach (t i in List_t)
                {
                    if (i.Class == s)
                        list.Add(i);
                }
                return list;
            }
            if (s == "Môn học")
            {
                foreach (t i in List_t)
                {
                    if (i.Subject == s)
                        list.Add(i);
                }
                return list;
            }

        }*/
    }
}